<section class="top-bar">
    <div class="top-bar__container">
        {% for i in (1..20) %}
        <div class="top-bar__item">
            <img class="top-bar__icon" src="/assets/star.svg" alt="" title="" width="17" height="17" loading="lazy">
            <span class="top-bar__text">{{settings['hero-banner'].settings.marquee_text |  upcase}}</span>
        </div>
        {% endfor %}
    </div>
</section>